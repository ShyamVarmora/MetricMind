function Loading() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "300px",
        fontSize: "22px",
        fontWeight: "600",
        color: "#2563eb",
      }}
    >
      Loading...
    </div>
  );
}

export default Loading;